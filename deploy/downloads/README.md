# Windows release directory

The self-hosted control center can read `latest.json` from this directory to display the current public version, size and SHA-256. Official installers belong in GitHub Releases and are downloaded from GitHub; they must never be committed here.
