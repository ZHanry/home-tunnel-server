# Windows release directory

The self-hosted control center can read an optional `latest.json` from this directory to display the current public version, size and SHA-256. Official installers belong in GitHub Releases and are downloaded from GitHub; server releases must never copy an `.exe` into this directory.
