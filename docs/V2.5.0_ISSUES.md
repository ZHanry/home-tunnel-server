# v2.5.0 milestone issue drafts

Create the labels listed in `docs/GITHUB_GOVERNANCE.md` before creating these Issues, then attach the real `v2.5.0` milestone. These drafts describe acceptance evidence; they do not claim that a remote Issue, setting, Release, or campaign result already exists.

## Dependency map

```text
01 security release ─┬─> 02 repository trust ─> 03 RC release evidence ─> 09 stable promotion
                     └─> 04 quality gates ─────┘
05 backend split ─┬─> 07 contracts and migrations ─> 03
06 clients/UI ────┘
08 Pages/launch ─────────────────────────────────────────────> 10 D30 review
09 stable promotion ─────────────────────────────────────────> 10 D30 review
```

## 01 — Publish v2.4.1 WebSocket security maintenance release

**Labels:** `security`, `fix`
**Dependencies:** none

### Body

Publish the WebSocket hardening as an isolated patch release before the broader v2.5.0 work. Pin `ws` to 8.21.3, retain the 64 KiB complete-message cap, enforce fragment and buffered-chunk limits, and keep compression disabled. Correct the supported-version table and private vulnerability-reporting link.

### Acceptance

- [ ] Production dependency audit reports no known High or Moderate vulnerability.
- [ ] Normal fragmented messages, oversize payloads, excessive fragments, excessive buffered chunks, unauthorized handshakes, abrupt disconnects, cleanup, and reconnect are covered.
- [ ] The v2.4.1 tag and assets contain only reviewed maintenance scope.
- [ ] Release notes state the supported versions and private reporting route accurately.

## 02 — Enable repository security settings and protected rulesets

**Labels:** `security`, `maintenance`
**Dependencies:** 01

### Body

Apply the owner-only settings in `docs/GITHUB_GOVERNANCE.md`: Private vulnerability reporting, Dependency graph, Dependabot alerts/security updates, retained Secret Scanning and Push Protection, main/tag rulesets, repository metadata, labels, and the real v2.5.0 milestone.

### Acceptance

- [ ] Owner-visible screenshots or API responses record each enabled security setting.
- [ ] Main requires PR, one approval, stale-approval dismissal, resolved discussions, stable required checks, linear history, and blocks force-push/deletion.
- [ ] `v*` tags cannot be updated or deleted; bypass is limited to the documented emergency path.
- [ ] Wiki/Projects are disabled; description, Homepage, Topics, social preview, labels, and milestone match the runbook.

## 03 — Prove the v2.5.0 RC artifact and release matrix

**Labels:** `maintenance`, `security`
**Dependencies:** 02, 04, 07

### Body

Run `v2.5.0-rc.2` through the complete release matrix. RC tags must remain prereleases. Stable images and assets must later be promoted from the same commit and verified artifact set, with no Windows `latest.json` while Windows distribution is suspended.

### Acceptance

- [ ] Linux server/client `amd64` and `arm64` artifacts are present and verified.
- [ ] macOS headless `amd64` and `arm64` Beta packages pass package-structure checks.
- [ ] SHA-256, SPDX SBOM, GitHub provenance, and keyless signature evidence exist for every published package/image in scope.
- [ ] Real Compose smoke plus HTTP/HTTPS/WebSocket, policy-revocation, backup/restore, Linux package self-check, and macOS structure checks pass before promotion.
- [ ] RC is marked prerelease and `latest` remains on the last fully verified stable release.

## 04 — Make Quality Gate the stable cross-language required check

**Labels:** `maintenance`, `dependencies`
**Dependencies:** 01

### Body

Keep one stable `Quality Gate` aggregation context while running Node 24, Go 1.26, and .NET 10 checks. Pin every GitHub Action to an immutable full commit SHA and analyze JavaScript/TypeScript, Go, and C# with CodeQL.

### Acceptance

- [ ] TypeScript check/lint/format/test/coverage and production audit pass for both services.
- [ ] Go race/vet/staticcheck/vulnerability and contract checks pass.
- [ ] xUnit, `dotnet format`, analyzers, restore audit, Compose config, Markdown, and link checks pass.
- [ ] Coverage floors are enforced at the documented control-center/gateway baselines; realtime WebSocket lines are at least 80%.
- [ ] The aggregator fails if any required job is failed, cancelled, or missing.

## 05 — Preserve protocol behavior while modularizing server components

**Labels:** `maintenance`, `enhancement`
**Dependencies:** 04

### Body

Keep the component-root repository layout. Split control-center administration by users, devices, connections, health, and audit; split the traffic gateway into configuration, policy, access control, rate limiting, sampling, proxying, observability, and server lifecycle modules.

### Acceptance

- [ ] Shared connection lookup and serialization logic has one tested implementation.
- [ ] Existing dependency-injection and test seams remain available.
- [ ] `/api/v1` paths, JSON fields, WebSocket event names, environment variables, and client discovery remain compatible.
- [ ] Existing and new TypeScript test suites pass without weakening coverage floors.

## 06 — Finish native frontend modules and standard client test services

**Labels:** `maintenance`, `enhancement`
**Dependencies:** 04

### Body

Keep the lightweight native frontend and split the browser script into ES Modules. Extract Windows update, session, realtime-sync, and Agent-coordination behavior into services, and keep Windows verification in a standard xUnit project.

### Acceptance

- [ ] No React/Vue or comparable framework is introduced.
- [ ] UI Preview and current public/admin behavior pass after module loading changes.
- [ ] Windows source builds with .NET 10 and analyzer warnings treated as errors.
- [ ] xUnit covers semantic versioning, update fallback, session/realtime behavior, and Agent coordination.
- [ ] Missing Windows `latest.json` safely degrades without an update failure loop.

## 07 — Lock cross-component contracts and append-only SQLite upgrades

**Labels:** `maintenance`, `fix`
**Dependencies:** 05, 06

### Body

Use shared fixtures for reserved words, config rendering, and REST/WebSocket messages without broadening Docker build contexts to share runtime code. Never rewrite migrations `001` through `006`; append new migrations only.

### Acceptance

- [ ] TypeScript and Go consumers validate the same versioned contract fixture.
- [ ] Windows/Linux render whitelists and Agent restrictions stay consistent.
- [ ] Earliest public SQLite databases upgrade through migration `007`.
- [ ] Verified backup restore is writable and a failed migration rolls back atomically.

## 08 — Publish bilingual Pages and owner-reviewed launch materials

**Labels:** `documentation`, `enhancement`
**Dependencies:** 04

### Body

Publish Chinese-first README/Pages and an English entry using real UI Preview screenshots. Configure privacy-preserving GoatCounter only after the owner provides `GOATCOUNTER_ENDPOINT`. Keep every external post or third-party PR behind immediate owner confirmation.

### Acceptance

- [ ] Pages includes bilingual positioning, quick start, support matrix, security model, FAQ, privacy, and feedback routes with no dynamic demo.
- [ ] Canonical, hreflang, Open Graph, JSON-LD, sitemap, robots, and 1280×640 social preview are valid.
- [ ] No broken links or mobile horizontal overflow; Lighthouse Accessibility/SEO/Best Practices ≥95 and Performance ≥90.
- [ ] D0/D1/D3/D5/D14/D30 review artifacts and UTM links match `docs/launch/` and the owner runbook.

## 09 — Promote the reviewed FRP 0.70.1 dependency and restricted Agent

**Labels:** `dependencies`, `security`
**Dependencies:** 03, 06, 07

### Body

Promote FRP 0.70.1 atomically across the restricted Agent, FRPS, active build pins, packaging, CodeQL and notices. Deploy the independently built `0.70.1-r1` FRPS image only by its reviewed immutable digest. Windows remains Source / Experimental: publish Agent provenance, but no EXE, MSIX or Windows update manifest until trusted signing and clean Windows 10/11 VM coverage exist. Follow `docs/FRP_0.70.1_COMPATIBILITY.md`; never land a partial pin update.

### Acceptance

- [x] The reviewed API adaptation and `-tags noweb` FRPS build change are applied atomically with every active pin and notice.
- [x] Protected run 31762807301 builds, audits, signs and attests the Go 1.26.6 `0.70.1-r1` image for `amd64` and `arm64`; deployment pins digest `sha256:cffde7b39698a5faba3828bb4a78b444d2d9c2cfea7385e28989728f5d73732f`.
- [x] Protected repository CI reproduces the fail-closed Agent SHA-256.
- [ ] The Release workflow publishes signed source-only Agent evidence without publishing the executable.
- [ ] Full Compose TLS/plugin/HTTP/TCP, whitelist, policy-revocation, Linux/macOS packaging, SBOM, provenance, checksum, and signature matrices pass.
- [ ] Windows 10/11 clean install and upgrade VM checks pass before official Windows binary distribution resumes; until then the unchecked gate is recorded and Windows artifacts stay absent.

## 10 — Complete the D30 growth validation and prioritize evidence-backed work

**Labels:** `documentation`, `maintenance`, `deployment-feedback`
**Dependencies:** 08 and a published stable release from 03

### Body

Review the 30-day validation goal using dated GoatCounter and GitHub Traffic snapshots. Do not infer users from clone counts contaminated by CI. Count only non-maintainer feedback containing a real platform, installation outcome, and success note or blocker.

### Acceptance

- [ ] D0, D7, D14, and D30 snapshots record Pages visitors, events, Stars, valid deployments, and aggregate referrers.
- [ ] Results are compared with 200 Pages unique visitors, 20 Stars, and 10 valid non-maintainer deployments.
- [ ] D14 decisions follow the predefined `<80 visitors` and `<15% Quick Start CTR` branches.
- [ ] Public retrospective contains aggregate evidence only and discloses no private referrer or deployment detail.
- [ ] The next milestone is ordered by observed deployment blockers, not vanity metrics.
